# deal with ANSI streaming gunk

options:

- strip it? lame
- xterm.js? rules on desktop, sucks on mobile
- hybrid? lame.
- there has to be a better way!

# terminal command

need to be able to do ! and switch to terminal mode to enter a command

# terminal mode (ala faux-term)

need to be able to do ! and press enter and switch to a model that looks like a
terminal, but isn't, executing each line like a terminal command, showing the
output, letting me do another command, etc. until I type the "exit" command.

# file attachments

need to be able to attach a file, e.g. image via copy paste, that goes to the
server-side

# @file mentions

need to be able to resolve @file mentions against the actual file system in
the cwd that the agent is running in

# /command executions

need to be able to get a set of commands from each agent and provide for
normal /command-style syntax completion
